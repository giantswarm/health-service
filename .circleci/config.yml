version: 2.1
orbs:
  architect: giantswarm/architect@dev:go-build

workflows:
  go-build:
    jobs:
      - architect/go-build:
          name: go-build-health-service
          binary: health-service
          # Needed to trigger job also on git tag.
          filters:
            tags:
              only: /^v.*/
